# C++11 核心特性学习笔记

> 本笔记整理 C++11 中常用的语法与语言特性。学习时不要只记语法，更要理解它们解决了什么问题。

## 一、基础语法增强

### 1.1 原始字符串字面量（Raw String Literal）

普通字符串中的反斜杠需要转义：

```cpp
#include <iostream>
#include <string>

int main()
{
    std::string path1 = "C:\\Users\\lic\\Desktop\\test.txt";
    std::string path2 = R"(C:\Users\lic\Desktop\test.txt)";

    std::cout << path1 << '\n';
    std::cout << path2 << '\n';
}
```

原始字符串的完整语法为：

```text
R"标记(字符串内容)标记"
```

“标记”可以省略，也可以自定义，但开始标记与结束标记必须一致：

```cpp
std::string html = R"html(
<html>
    <body>
        <h1>Hello C++</h1>
    </body>
</html>
)html";

std::string json = R"json(
{
    "name": "lic",
    "message": "hello"
}
)json";
```

注意：原始字符串中的 `\n` 是反斜杠和字母 `n`，不会被解释成换行符。

适用场景：Windows 路径、正则表达式、JSON、HTML、SQL 和多行文本。

---

### 1.2 `long long`

C++11 正式引入 `long long` 和 `unsigned long long`。标准保证 `long long` 至少为 64 位，但具体大小仍应通过 `sizeof` 或类型限制进行确认。

```cpp
#include <climits>
#include <iostream>
#include <limits>

int main()
{
    std::cout << sizeof(long long) << '\n';
    std::cout << LLONG_MIN << '\n';
    std::cout << LLONG_MAX << '\n';
    std::cout << ULLONG_MAX << '\n';

    std::cout << std::numeric_limits<long long>::min() << '\n';
    std::cout << std::numeric_limits<long long>::max() << '\n';
}
```

常用字面量后缀：

```cpp
long long a = 123LL;
unsigned long long b = 123ULL;
```

注意：`long` 的大小与平台有关。例如，64 位 Windows 中通常为 4 字节，64 位 Linux 中通常为 8 字节。

---

### 1.3 非静态数据成员的默认初始化

C++11 允许在类中直接为非静态数据成员设置默认初始值：

```cpp
#include <string>

class Person
{
public:
    Person() = default;

    Person(int age, const std::string& name)
        : _age(age), _name(name)  // 覆盖类内默认值
    {
    }

private:
    int _age{18};
    double _height{175.0};
    char _level{'A'};
    std::string _name{"unknown"};
};
```

规则：

- 构造函数没有显式初始化该成员时，使用类内默认值。
- 构造函数初始化列表提供了值时，以初始化列表为准。
- 该语法减少了多个构造函数之间重复的初始化代码。

静态数据成员不属于某个具体对象，通常仍需在类外定义：

```cpp
class Config
{
public:
    static const int maxCount = 100; // 整型常量可在类内给出常量初始化值
    static double rate;
};

double Config::rate = 0.8;
```

注意：在 C++11 中，如果 `static const int` 被 ODR-use（例如对它取地址），通常还需要在类外提供定义。

---

### 1.4 `final`

`final` 有两种用途。

#### 用途一：禁止继续重写虚函数

```cpp
class Base
{
public:
    virtual void test() const
    {
    }

    virtual ~Base() = default;
};

class Child : public Base
{
public:
    void test() const override final
    {
    }
};
```

`Child::test()` 之后不能再被派生类重写。

#### 用途二：禁止类继续被继承

```cpp
class FinalClass final : public Base
{
};

// class Other : public FinalClass {}; // 编译错误
```

易错点：类被 `final` 修饰，表示该类不能被继承，不是“不能被重写”。

---

### 1.5 `override`

`override` 告诉编译器：当前函数必须重写基类的虚函数。如果函数签名不匹配，编译器会立即报错。

```cpp
class Base
{
public:
    virtual void print() const
    {
    }

    virtual ~Base() = default;
};

class Derived : public Base
{
public:
    void print() const override
    {
    }
};
```

下面的代码会报错，因为缺少 `const`，没有真正重写基类函数：

```cpp
class Error : public Base
{
public:
    // void print() override {} // 编译错误
};
```

建议：派生类重写虚函数时始终写 `override`。写了 `override` 后，可以省略前面的 `virtual`。

---

### 1.6 嵌套模板的右尖括号

C++11 之前，嵌套模板的两个右尖括号之间通常需要空格，否则可能被解析为右移运算符。

```cpp
#include <vector>

// C++98 常见写法
std::vector<std::vector<int> > oldStyle;

// C++11 写法
std::vector<std::vector<int>> newStyle;
```

---

### 1.7 模板默认参数

#### 类模板默认参数

```cpp
template<class T = int, class U = double>
class Data
{
public:
    T first{};
    U second{};
};

Data<> d1;           // Data<int, double>
Data<long> d2;       // Data<long, double>
Data<long, float> d3;
```

#### 函数模板默认参数

C++11 允许函数模板参数带默认类型：

```cpp
#include <iostream>

template<class T = long, class U = char>
void print(T value = 123, U separator = '|')
{
    std::cout << value << ' ' << separator << '\n';
}

int main()
{
    print();                         // T=long，U=char
    print(10);                       // T 由实参推导为 int，U 使用 char
    print<double>(3.14);             // 显式指定 T，U 使用 char
    print<double, int>(3.14, 100);   // 两个类型都显式指定
}
```

要区分两种默认值：

- `T = long`：模板类型参数的默认值。
- `value = 123`：函数形参的默认值。

传入实参时，函数模板优先根据实参推导对应类型；无法推导时才可能使用模板默认类型。

---

### 1.8 `static_assert` 静态断言

运行时断言 `assert` 在程序运行到对应位置时检查条件：

```cpp
#include <cassert>

char* createArray(int size)
{
    assert(size > 0);
    return new char[size];
}
```

静态断言 `static_assert` 在编译阶段检查常量表达式：

```cpp
#include <cstdint>

static_assert(sizeof(std::int64_t) == 8,
              "int64_t must occupy 8 bytes");
```

语法：

```cpp
static_assert(常量表达式, "错误提示信息");
```

| 对比项 | `assert` | `static_assert` |
| --- | --- | --- |
| 检查时间 | 运行期 | 编译期 |
| 条件 | 运行时表达式 | 常量表达式 |
| 失败结果 | 程序终止 | 编译失败 |
| 头文件 | `<cassert>` | 无需专门头文件 |

---

### 1.9 `noexcept`

`noexcept` 用于声明函数不会让异常逃出函数体：

```cpp
void func() noexcept
{
    // 函数承诺异常不会从这里传播出去
}
```

等价关系：

```cpp
void f1() noexcept(true);   // 不允许异常逃出
void f2() noexcept(false);  // 允许异常传播
```

原笔记中的含义需要反过来理解：`true` 表示“不抛出”，`false` 表示“可能抛出”。

如果异常从 `noexcept(true)` 函数中逃出，程序会调用 `std::terminate()`，外层 `try-catch` 无法正常接住它。

```cpp
#include <stdexcept>

void dangerous() noexcept
{
    throw std::runtime_error("error"); // 将导致 std::terminate()
}
```

在函数内部抛出后又被内部的 `catch` 完全处理，不算异常逃出函数。

还可以使用 `noexcept(表达式)` 进行条件声明，并通过 `noexcept(...)` 运算符判断一个表达式是否承诺不抛异常：

```cpp
void safe() noexcept
{
}

static_assert(noexcept(safe()), "safe should not throw");
```

常见用途：析构函数、移动构造函数、移动赋值函数以及对异常安全要求较高的底层接口。

---

### 1.10 字符串与数值转换

#### 数值转换为字符串

```cpp
#include <string>

std::string s1 = std::to_string(123);
std::string s2 = std::to_string(3.14);
```

`std::to_string` 支持常见的有符号、无符号整数类型和浮点类型。

#### 字符串转换为数值

```cpp
std::stoi()   // string -> int
std::stol()   // string -> long
std::stoll()  // string -> long long

std::stoul()  // string -> unsigned long
std::stoull() // string -> unsigned long long

std::stof()   // string -> float
std::stod()   // string -> double
std::stold()  // string -> long double
```

完整示例：

```cpp
#include <iostream>
#include <string>

int main()
{
    std::size_t pos = 0;
    int value = std::stoi("123abc", &pos);

    std::cout << value << '\n'; // 123
    std::cout << pos << '\n';   // 3，表示成功转换了几个字符
}
```

注意：转换失败可能抛出 `std::invalid_argument`；数值超出目标类型范围可能抛出 `std::out_of_range`。

---

## 二、类型推导与便捷语法

### 2.1 `auto`

`auto` 根据初始化表达式推导变量类型，因此普通 `auto` 变量必须初始化。

```cpp
auto a = 10;       // int
auto b = 3.14;     // double
auto c = 3.14f;    // float
auto text = "abc"; // const char*
```

#### `const` 与引用的推导规则

```cpp
const int value = 10;
const int& ref = value;

auto a = value;        // int：丢弃顶层 const
auto b = ref;          // int：丢弃引用和顶层 const
const auto c = value;  // const int
auto& d = value;       // const int&
const auto& e = value; // const int&
```

数组在按值推导时会退化成指针，通过引用推导则可以保留数组类型：

```cpp
int array[3] = {1, 2, 3};

auto p = array;   // int*
auto& r = array;  // int (&)[3]
```

#### 常见应用：简化迭代器类型

```cpp
#include <iostream>
#include <map>
#include <string>

int main()
{
    std::map<int, std::string> dict{
        {1, "one"},
        {2, "two"},
        {3, "three"}
    };

    for (auto it = dict.begin(); it != dict.end(); ++it)
    {
        std::cout << it->first << ':' << it->second << '\n';
    }
}
```

C++11 中的主要限制：

- 普通变量必须有初始化表达式。
- 不能使用 `auto` 作为普通函数形参类型；泛型 Lambda 的 `auto` 形参是 C++14 特性。
- 非静态数据成员不能直接声明为 `auto`。
- 不能仅写 `auto array[10];` 声明数组。
- `auto` 非类型模板参数是 C++17 特性。

---

### 2.2 `decltype`

`decltype` 根据变量或表达式推导类型，但不会真正执行该表达式。

#### 规则一：未加括号的变量名或成员访问

得到该实体声明时的准确类型：

```cpp
const int value = 10;
const int& ref = value;

decltype(value) a = 20; // const int
decltype(ref) b = a;    // const int&
```

#### 规则二：其他表达式根据值类别推导

- 左值表达式得到 `T&`。
- 将亡值表达式得到 `T&&`。
- 纯右值表达式得到 `T`。

```cpp
int x = 10;
int y = 20;

decltype((x)) r = x;       // int&，因为 (x) 是左值表达式
decltype(x + y) sum = 0;   // int，x + y 是纯右值
decltype(x = y) ref2 = x;  // int&，赋值表达式返回左值
```

最重要的区别：

```cpp
decltype(x)    // 得到 x 的声明类型：int
decltype((x))  // 按表达式值类别推导：int&
```

#### C++11 尾置返回类型

当函数返回类型依赖形参表达式时，可以把返回类型写在形参列表后面：

```cpp
template<class T, class U>
auto add(const T& left, const U& right) -> decltype(left + right)
{
    return left + right;
}
```

使用 `decltype` 获取容器迭代器类型时，可以借助 `std::declval`，避免要求容器必须能够默认构造：

```cpp
#include <utility>

template<class Container>
class Printer
{
private:
    using Iterator = decltype(std::declval<const Container&>().cbegin());
    Iterator _it;
};
```

#### `auto` 与 `decltype` 的区别

| 对比项 | `auto` | `decltype` |
| --- | --- | --- |
| 主要作用 | 声明变量并推导类型 | 获取变量或表达式的类型 |
| 是否需要初始化 | 普通变量必须初始化 | 不需要执行表达式 |
| 顶层 `const` | 按值推导时通常丢弃 | 通常准确保留 |
| 引用 | 按值推导时通常丢弃 | 可根据规则保留或生成引用 |

---

### 2.3 范围 `for`

范围 `for` 可以遍历数组和支持 `begin()`、`end()` 的容器。

```cpp
#include <iostream>
#include <vector>

int main()
{
    std::vector<int> values{1, 2, 3, 4, 5};

    for (int value : values)          // 按值读取
    {
        std::cout << value << ' ';
    }

    for (int& value : values)         // 引用，可修改原元素
    {
        ++value;
    }

    for (const int& value : values)   // 只读引用，避免复制
    {
        std::cout << value << ' ';
    }
}
```

遍历 `map`：

```cpp
#include <iostream>
#include <map>
#include <string>

std::map<int, std::string> dict{{1, "one"}, {2, "two"}};

for (const auto& entry : dict)
{
    std::cout << entry.first << ':' << entry.second << '\n';
}
```

选择原则：

- 只读基本类型：`for (auto value : container)`。
- 只读大对象：`for (const auto& value : container)`。
- 需要修改元素：`for (auto& value : container)`。

注意：循环体内不要随意执行会使迭代器失效的插入、删除或扩容操作。

---

### 2.4 `nullptr`

`nullptr` 是 C++11 引入的空指针字面量，类型为 `std::nullptr_t`。

```cpp
#include <iostream>

void func(int)
{
    std::cout << "func(int)\n";
}

void func(char*)
{
    std::cout << "func(char*)\n";
}

int main()
{
    func(0);        // 调用 func(int)
    func(nullptr);  // 调用 func(char*)

    int* p1 = nullptr;
    char* p2 = nullptr;
}
```

注意：在 C++ 中，`NULL` 是实现定义的空指针常量，通常被定义为 `0` 或 `0L`，不是 C 风格的 `(void*)0`。重载场景中应优先使用 `nullptr`，避免整数与指针之间产生歧义。

---

### 2.5 Lambda 表达式

Lambda 是可以在使用位置直接定义的匿名函数对象。

完整语法：

```text
[捕获列表](参数列表) mutable noexcept -> 返回类型
{
    函数体
}
```

除捕获列表和函数体外，其他部分可按需省略。

#### 基础用法

```cpp
#include <iostream>

int main()
{
    auto add = [](int x, int y) -> int
    {
        return x + y;
    };

    std::cout << add(2, 4) << '\n';
}
```

#### 捕获方式

```cpp
int a = 10;
int b = 20;

auto f1 = [a]() { return a; };          // 按值捕获 a
auto f2 = [&a]() { ++a; };              // 按引用捕获 a
auto f3 = [=]() { return a + b; };       // 默认按值捕获使用到的局部变量
auto f4 = [&]() { ++a; ++b; };           // 默认按引用捕获使用到的局部变量
auto f5 = [a, &b]() { ++b; return a; };  // a 按值，b 按引用
```

| 捕获形式 | 含义 |
| --- | --- |
| `[]` | 不捕获外部局部变量 |
| `[a]` | 按值捕获 `a` |
| `[&a]` | 按引用捕获 `a` |
| `[=]` | 默认按值捕获 |
| `[&]` | 默认按引用捕获 |
| `[this]` | 捕获当前对象的 `this` 指针 |

#### `mutable`

按值捕获的变量默认不能在 Lambda 内修改。`mutable` 允许修改 Lambda 对象内部保存的副本，不会修改外部原变量：

```cpp
int count = 0;

auto next = [count]() mutable
{
    return ++count;
};

next(); // 返回 1
next(); // 返回 2
// 外部 count 仍然是 0
```

#### 立即调用

```cpp
int x = 10;
int y = 20;

int result = [=]()
{
    return x + y;
}();
```

#### 配合 STL 算法

```cpp
#include <algorithm>
#include <vector>

std::vector<int> values{5, 2, 8, 1};

std::sort(values.begin(), values.end(),
          [](int left, int right)
          {
              return left > right;
          });
```

注意：按引用捕获时，Lambda 的生命周期不能超过被捕获变量的生命周期，否则可能形成悬空引用。

---

## 三、编译期计算与类设计

### 3.1 `constexpr`

`const` 表示对象不可通过当前名字修改；`constexpr` 进一步要求对象可以在编译期得到常量值。

```cpp
constexpr int count = 24;
int array[count]{};
```

初始化 `constexpr` 变量的表达式必须是常量表达式：

```cpp
int runtimeValue = 10;

const int a = runtimeValue;      // 可以：只表示 a 之后不可修改
// constexpr int b = runtimeValue; // 错误：不是编译期常量
```

#### `constexpr` 函数

```cpp
constexpr int add(int x, int y)
{
    return x + y;
}

constexpr int result = add(10, 20); // 编译期计算
int runtimeResult = add(10, 20);     // 也可以在运行期使用
```

`constexpr` 函数不代表每次调用都一定在编译期计算；当实参是常量表达式且上下文要求常量时，才可以用于编译期求值。

C++11 对 `constexpr` 函数体限制较严格，通常只能包含一条 `return` 语句；C++14 才明显放宽这些限制。

#### `constexpr` 与类

`constexpr` 不仅能修饰内置类型，也能用于满足“字面类型”要求的类对象：

```cpp
class Point
{
public:
    constexpr Point(int x, int y)
        : _x(x), _y(y)
    {
    }

    constexpr int x() const
    {
        return _x;
    }

private:
    int _x;
    int _y;
};

constexpr Point point(10, 20);
static_assert(point.x() == 10, "unexpected x");
```

因此，“`constexpr` 只能修饰内置类型，不能修饰 `struct` 或 `class`”是错误的。

---

### 3.2 `using` 类型别名

`using` 可以替代 `typedef` 定义类型别名，语义更直观：

```cpp
typedef unsigned long ulong1;
using ulong2 = unsigned long;

typedef int (*FuncPtr1)(int, double);
using FuncPtr2 = int (*)(int, double);
```

`using` 的重要优势是能够直接定义别名模板：

```cpp
#include <map>
#include <string>

template<class T>
using StringMap = std::map<std::string, T>;

StringMap<int> scores;
StringMap<double> prices;
```

而使用 `typedef` 实现类似效果时，通常需要再包一层类模板：

```cpp
template<class T>
struct StringMapOld
{
    typedef std::map<std::string, T> type;
};
```

结论：普通别名两者都可以；现代 C++ 通常优先使用 `using`，模板别名则应使用 `using`。

---

### 3.3 委托构造函数

委托构造是“同一个类中的一个构造函数调用另一个构造函数”完成公共初始化。

```cpp
#include <string>
#include <utility>

class Student
{
public:
    Student()
        : Student(0, "unknown")
    {
    }

    explicit Student(int id)
        : Student(id, "unknown")
    {
    }

    Student(int id, std::string name)
        : _id(id), _name(std::move(name))
    {
    }

private:
    int _id;
    std::string _name;
};
```

其中 `Student(int, std::string)` 是目标构造函数，其他构造函数是委托构造函数。

注意：

- 委托必须写在成员初始化列表中。
- 一旦委托给其他构造函数，就不能再在同一个初始化列表中初始化其他成员。
- 构造函数之间不能形成循环委托。
- 目标构造函数先完成，随后执行委托构造函数自己的函数体。

---

### 3.4 继承构造函数

C++11 允许派生类通过 `using Base::Base` 继承基类构造函数：

```cpp
#include <string>

class Base
{
public:
    Base(int id, const std::string& name)
        : _id(id), _name(name)
    {
    }

private:
    int _id;
    std::string _name;
};

class Derived : public Base
{
public:
    using Base::Base;

private:
    int _level{0};
};

int main()
{
    Derived object(1, "lic");
}
```

注意：

- 继承的是构造方式，不是把基类构造函数变成派生类的普通成员函数。
- 派生类自己的成员仍会按照默认成员初始化器或默认初始化规则处理。
- 如果派生类需要额外的初始化逻辑，应自行定义构造函数。
- 构造函数不能是虚函数；析构函数也不会通过 `using Base::Base` 被继承。

---

## 四、核心知识总结

| 特性 | 解决的问题 | 最关键规则 |
| --- | --- | --- |
| 原始字符串 | 减少转义，方便保存多行文本 | `R"tag(...)tag"` 两端标记一致 |
| `long long` | 提供至少 64 位的整数类型 | 使用 `LL`、`ULL` 后缀 |
| 类内成员初始化 | 减少构造函数中的重复初始化 | 构造函数初始化列表可以覆盖默认值 |
| `final` | 阻止继续继承或重写 | 修饰类表示禁止继承，修饰虚函数表示禁止重写 |
| `override` | 让编译器检查虚函数重写 | 签名不匹配会编译失败 |
| 模板 `>>` | 简化嵌套模板写法 | C++11 不再强制在两个 `>` 之间加空格 |
| 模板默认参数 | 为模板类型提供默认选择 | 与函数形参默认值是两套机制 |
| `static_assert` | 在编译期检查约束 | 条件必须是常量表达式 |
| `noexcept` | 声明异常不会逃出函数 | `true` 表示不抛，违反承诺会 `terminate` |
| 数值转换函数 | 简化字符串与数值互转 | 注意转换异常和有效字符位置 |
| `auto` | 简化复杂类型声明 | 按值推导通常丢弃顶层 `const` 和引用 |
| `decltype` | 精确获取变量或表达式类型 | `decltype(x)` 与 `decltype((x))` 可能不同 |
| 范围 `for` | 简化容器遍历 | 修改元素用引用，大对象只读用常量引用 |
| `nullptr` | 提供类型安全的空指针 | 重载场景中优于 `0` 和 `NULL` |
| Lambda | 就地创建匿名函数对象 | 注意捕获方式和引用生命周期 |
| `constexpr` | 支持编译期常量和计算 | 函数能否编译期求值取决于实参和上下文 |
| `using` | 简化类型别名 | 支持别名模板 |
| 委托构造 | 复用同一个类的构造逻辑 | 不能循环委托或同时初始化其他成员 |
| 继承构造 | 复用基类构造接口 | 派生类新增成员仍需正确初始化 |

## 五、常见易错点

1. `noexcept(true)` 表示不允许异常逃出函数，而不是允许抛出异常。
2. `final` 修饰类时表示禁止继承；只有修饰虚函数时才表示禁止继续重写。
3. `override` 检查的是完整函数签名，参数、`const` 和引用限定符等都必须匹配。
4. `auto` 按值推导通常不会保留引用和顶层 `const`；需要时应显式写 `auto&` 或 `const auto&`。
5. `decltype(x)` 获取声明类型，`decltype((x))` 按表达式推导，结果经常是引用。
6. `NULL` 通常只是整数零形式的空指针常量，现代 C++ 应优先使用 `nullptr`。
7. `constexpr` 不等于“永远在编译期执行”，它只是让函数或对象具备参与常量表达式的能力。
8. Lambda 按引用捕获时，必须确保被捕获对象仍然存活。

## 六、后续建议补充的 C++11 主题

当前笔记主要覆盖语法增强。要系统掌握 C++11，后续还应重点学习：

1. 右值引用、移动语义与完美转发：`T&&`、`std::move`、`std::forward`。
2. 智能指针：`std::unique_ptr`、`std::shared_ptr`、`std::weak_ptr`。
3. 可变参数模板和参数包展开。
4. `= default`、`= delete` 与特殊成员函数。
5. `std::function`、`std::bind` 和可调用对象。
6. 多线程库：`std::thread`、互斥量、条件变量和原子操作。
7. 新增 STL 容器与工具：`std::array`、`std::unordered_map`、`std::tuple` 等。

这些内容，尤其是移动语义、智能指针和并发库，是 C++ 后端开发与面试中的重点。
