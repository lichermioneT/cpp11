异步线程

# C++11 异步线程总结

C++11 中的“异步线程”通常指使用 `<future>` 提供的异步编程工具，让一个任务在后台执行，并在未来某个时刻获取执行结果。

核心组件包括：

- `std::async`
- `std::future`
- `std::promise`
- `std::packaged_task`
- `std::shared_future`

------

## 一、为什么需要异步编程

使用 `std::thread` 创建线程时，通常需要手动处理：

- 线程创建
- 参数传递
- `join()` 或 `detach()`
- 返回值获取
- 异常传递
- 线程同步

例如，普通线程不能直接返回结果：

```cpp
#include <iostream>
#include <thread>

void add(int a, int b, int& result)
{
    result = a + b;
}

int main()
{
    int result = 0;

    std::thread t(add, 10, 20, std::ref(result));
    t.join();

    std::cout << result << std::endl;

    return 0;
}
```

而使用异步任务，可以直接获取返回值：

```cpp
#include <iostream>
#include <future>

int add(int a, int b)
{
    return a + b;
}

int main()
{
    std::future<int> result = std::async(add, 10, 20);

    std::cout << result.get() << std::endl;

    return 0;
}
```

------

# 二、`std::async`

## 1. 基本作用

`std::async` 用来异步执行一个可调用对象，并返回一个 `std::future`。

```cpp
std::future<返回值类型> future对象 =
    std::async(函数, 参数...);
```

示例：

```cpp
#include <iostream>
#include <future>
#include <thread>
#include <chrono>

int calculate(int a, int b)
{
    std::this_thread::sleep_for(std::chrono::seconds(2));
    return a + b;
}

int main()
{
    std::future<int> result =
        std::async(calculate, 10, 20);

    std::cout << "主线程继续执行" << std::endl;

    std::cout << "计算结果：" << result.get() << std::endl;

    return 0;
}
```

程序逻辑：

```text
主线程启动异步任务
        ↓
主线程继续执行
        ↓
调用 future.get()
        ↓
如果任务未完成，主线程阻塞等待
        ↓
获取异步任务返回值
```

------

## 2. `std::async` 的启动策略

`std::async` 可以指定任务如何执行。

### `std::launch::async`

要求任务异步执行，通常会创建一个新线程。

```cpp
std::future<int> result =
    std::async(std::launch::async, calculate, 10, 20);
```

特点：

- 任务立即开始执行
- 通常由另一个线程完成
- 适合真正的并行任务

------

### `std::launch::deferred`

延迟执行。

```cpp
std::future<int> result =
    std::async(std::launch::deferred, calculate, 10, 20);
```

此时任务不会立即运行。

只有调用下面这些函数时，任务才会在当前线程中执行：

```cpp
result.get();
result.wait();
```

示例：

```cpp
#include <iostream>
#include <future>
#include <thread>

int task()
{
    std::cout << "任务线程：" << std::this_thread::get_id() << '\n';
    return 100;
}

int main()
{
    std::cout << "主线程：" << std::this_thread::get_id() << '\n';

    std::future<int> result =
        std::async(std::launch::deferred, task);

    std::cout << "任务还没有执行" << '\n';

    std::cout << result.get() << '\n';

    return 0;
}
```

这里的 `task()` 会在调用 `get()` 的线程中执行，也就是主线程。

------

### 默认启动策略

```cpp
std::async(task);
```

等价于允许实现自行选择：

```cpp
std::launch::async | std::launch::deferred
```

也就是说，编译器或标准库可能：

- 创建新线程执行
- 延迟到 `get()` 或 `wait()` 时执行

为了行为明确，通常建议显式指定：

```cpp
std::async(std::launch::async, task);
```

------

# 三、`std::future`

## 1. 基本作用

`std::future` 表示一个“未来才会得到的结果”。

可以把它理解为：

```text
异步任务结果的接收端
```

常见来源：

- `std::async`
- `std::promise::get_future()`
- `std::packaged_task::get_future()`

------

## 2. `future.get()`

获取异步结果。

```cpp
int value = result.get();
```

特点：

- 如果任务未完成，当前线程会阻塞等待
- 如果任务完成，返回执行结果
- 只能调用一次
- 调用后，`future` 不再关联共享状态

示例：

```cpp
std::future<int> result = std::async([] {
    return 100;
});

int value = result.get();
```

不能连续调用：

```cpp
std::cout << result.get() << '\n';
std::cout << result.get() << '\n';  // 错误
```

第二次调用通常会抛出：

```cpp
std::future_error
```

------

## 3. `future.valid()`

判断 `future` 是否关联了有效的共享状态。

```cpp
if (result.valid())
{
    std::cout << result.get() << '\n';
}
```

调用 `get()` 后：

```cpp
std::future<int> result = std::async([] {
    return 100;
});

std::cout << result.valid() << '\n'; // true

result.get();

std::cout << result.valid() << '\n'; // false
```

------

## 4. `future.wait()`

等待任务完成，但不获取返回值。

```cpp
result.wait();
```

区别：

```cpp
result.wait();  // 只等待
result.get();   // 等待并获取结果
```

示例：

```cpp
std::future<int> result = std::async(std::launch::async, [] {
    return 100;
});

result.wait();

std::cout << "任务已经完成" << '\n';
std::cout << result.get() << '\n';
```

------

## 5. `future.wait_for()`

等待指定时间。

```cpp
result.wait_for(std::chrono::seconds(1));
```

返回值是：

```cpp
std::future_status
```

可能的状态：

```cpp
std::future_status::ready
std::future_status::timeout
std::future_status::deferred
```

示例：

```cpp
#include <iostream>
#include <future>
#include <chrono>
#include <thread>

int task()
{
    std::this_thread::sleep_for(std::chrono::seconds(3));
    return 100;
}

int main()
{
    std::future<int> result =
        std::async(std::launch::async, task);

    std::future_status status =
        result.wait_for(std::chrono::seconds(1));

    if (status == std::future_status::ready)
    {
        std::cout << "任务完成：" << result.get() << '\n';
    }
    else if (status == std::future_status::timeout)
    {
        std::cout << "等待超时" << '\n';
    }
    else if (status == std::future_status::deferred)
    {
        std::cout << "任务被延迟执行" << '\n';
    }

    return 0;
}
```

------

## 6. `future.wait_until()`

等待到某个具体时间点。

```cpp
auto timePoint =
    std::chrono::steady_clock::now() +
    std::chrono::seconds(2);

result.wait_until(timePoint);
```

`wait_for()` 使用相对时间：

```cpp
等待两秒
```

`wait_until()` 使用绝对时间点：

```cpp
等待到某个时刻
```

------

# 四、异步任务的异常传递

`std::async` 中的异常不会直接在异步线程中传播到主线程。

异常会存放在共享状态中，调用 `future.get()` 时重新抛出。

```cpp
#include <iostream>
#include <future>
#include <stdexcept>

int task()
{
    throw std::runtime_error("异步任务发生异常");
}

int main()
{
    std::future<int> result =
        std::async(std::launch::async, task);

    try
    {
        int value = result.get();
    }
    catch (const std::exception& e)
    {
        std::cout << "捕获异常：" << e.what() << '\n';
    }

    return 0;
}
```

这也是 `future` 相比普通 `std::thread` 的重要优势。

------

# 五、`std::promise`

## 1. 基本作用

`std::promise` 用于在线程之间传递一个值或异常。

可以理解为：

```text
promise：结果生产端
future：结果接收端
```

它们通过共享状态连接：

```text
线程A
promise.set_value()
        ↓
    共享状态
        ↓
future.get()
线程B
```

------

## 2. 基本用法

```cpp
#include <iostream>
#include <future>
#include <thread>

void calculate(std::promise<int>& promise)
{
    int result = 10 + 20;
    promise.set_value(result);
}

int main()
{
    std::promise<int> promise;

    std::future<int> future = promise.get_future();

    std::thread t(calculate, std::ref(promise));

    std::cout << "结果：" << future.get() << '\n';

    t.join();

    return 0;
}
```

------

## 3. `set_value()`

向共享状态中写入结果。

```cpp
promise.set_value(100);
```

对应：

```cpp
int result = future.get();
```

------

## 4. 传递异常

可以使用：

```cpp
promise.set_exception()
```

示例：

```cpp
#include <iostream>
#include <future>
#include <thread>
#include <stdexcept>

void task(std::promise<int>& promise)
{
    try
    {
        throw std::runtime_error("任务失败");

        promise.set_value(100);
    }
    catch (...)
    {
        promise.set_exception(std::current_exception());
    }
}

int main()
{
    std::promise<int> promise;
    std::future<int> future = promise.get_future();

    std::thread t(task, std::ref(promise));

    try
    {
        std::cout << future.get() << '\n';
    }
    catch (const std::exception& e)
    {
        std::cout << "捕获异常：" << e.what() << '\n';
    }

    t.join();

    return 0;
}
```

------

## 5. `std::promise<void>`

如果只需要通知任务完成，不需要返回数据，可以使用：

```cpp
std::promise<void>
```

示例：

```cpp
#include <iostream>
#include <thread>
#include <future>

void task(std::promise<void>& promise)
{
    std::cout << "执行任务" << '\n';

    promise.set_value();
}

int main()
{
    std::promise<void> promise;
    std::future<void> future = promise.get_future();

    std::thread t(task, std::ref(promise));

    future.get();

    std::cout << "任务完成" << '\n';

    t.join();

    return 0;
}
```

------

# 六、`std::packaged_task`

## 1. 基本作用

`std::packaged_task` 可以把一个可调用对象包装成异步任务。

可包装：

- 普通函数
- Lambda 表达式
- 函数对象
- 成员函数调用包装
- `std::bind` 返回对象

它会自动将函数返回值或异常存入共享状态。

------

## 2. 基本用法

```cpp
#include <iostream>
#include <future>
#include <thread>

int add(int a, int b)
{
    return a + b;
}

int main()
{
    std::packaged_task<int(int, int)> task(add);

    std::future<int> future = task.get_future();

    std::thread t(std::move(task), 10, 20);

    std::cout << future.get() << '\n';

    t.join();

    return 0;
}
```

注意：

```cpp
std::packaged_task
```

通常不可复制，只能移动，所以传入线程时要使用：

```cpp
std::move(task)
```

------

## 3. 使用 Lambda

```cpp
std::packaged_task<int(int, int)> task(
    [](int a, int b) {
        return a + b;
    }
);
```

完整示例：

```cpp
#include <iostream>
#include <future>
#include <thread>

int main()
{
    std::packaged_task<int(int, int)> task(
        [](int a, int b) {
            return a + b;
        }
    );

    std::future<int> future = task.get_future();

    std::thread t(std::move(task), 10, 20);

    std::cout << future.get() << '\n';

    t.join();

    return 0;
}
```

------

# 七、`std::shared_future`

## 1. 为什么需要 `shared_future`

普通 `future`：

- 只能调用一次 `get()`
- 通常只能被一个线程消费

而 `shared_future`：

- 可以多次调用 `get()`
- 可以被复制
- 多个线程可以共享同一个异步结果

------

## 2. 基本用法

```cpp
#include <iostream>
#include <future>
#include <thread>

int main()
{
    std::future<int> future =
        std::async(std::launch::async, [] {
            return 100;
        });

    std::shared_future<int> shared = future.share();

    std::thread t1([shared] {
        std::cout << "线程1：" << shared.get() << '\n';
    });

    std::thread t2([shared] {
        std::cout << "线程2：" << shared.get() << '\n';
    });

    t1.join();
    t2.join();

    return 0;
}
```

调用：

```cpp
future.share();
```

之后，原来的 `future` 会失效。

------

# 八、返回 `void` 的异步任务

如果异步函数没有返回值，使用：

```cpp
std::future<void>
```

示例：

```cpp
#include <iostream>
#include <future>

void print()
{
    std::cout << "异步任务执行" << '\n';
}

int main()
{
    std::future<void> future =
        std::async(std::launch::async, print);

    future.get();

    return 0;
}
```

虽然没有返回值，但 `get()` 仍然有意义：

- 等待任务完成
- 接收异步任务中的异常

------

# 九、异步执行成员函数

```cpp
#include <iostream>
#include <future>

class Calculator
{
public:
    int add(int a, int b)
    {
        return a + b;
    }
};

int main()
{
    Calculator calculator;

    std::future<int> result =
        std::async(
            std::launch::async,
            &Calculator::add,
            &calculator,
            10,
            20
        );

    std::cout << result.get() << '\n';

    return 0;
}
```

参数说明：

```cpp
&Calculator::add   // 成员函数地址
&calculator        // 对象地址
10, 20             // 成员函数参数
```

也可以传引用：

```cpp
std::ref(calculator)
```

------

# 十、`std::async` 与 `std::thread` 的区别

| 对比项   | `std::thread`              | `std::async`           |
| -------- | -------------------------- | ---------------------- |
| 返回值   | 不能直接获取               | 通过 `future` 获取     |
| 异常传递 | 需要手动处理               | `get()` 时自动重新抛出 |
| 线程管理 | 手动 `join()` / `detach()` | 通常自动管理           |
| 启动方式 | 创建独立线程               | 可能异步，也可能延迟   |
| 使用难度 | 较高                       | 较低                   |
| 灵活性   | 更高                       | 相对简单               |
| 适用场景 | 长期线程、复杂线程控制     | 有返回值的独立任务     |

------

# 十一、`async`、`promise`、`packaged_task` 的区别

| 工具                 | 主要用途                     |
| -------------------- | ---------------------------- |
| `std::async`         | 最简单地启动异步任务         |
| `std::future`        | 获取未来结果                 |
| `std::promise`       | 手动设置结果或异常           |
| `std::packaged_task` | 将函数包装为可异步执行的任务 |
| `std::shared_future` | 多线程共享同一个结果         |

使用原则：

```text
只想异步调用函数
        ↓
使用 std::async

已经手动创建线程，需要在线程间传递结果
        ↓
使用 std::promise + std::future

需要把任务保存、放入队列、延迟执行
        ↓
使用 std::packaged_task

多个线程需要读取同一个异步结果
        ↓
使用 std::shared_future
```

------

# 十二、异步任务与线程池

`std::packaged_task` 特别适合在线程池中使用。

线程池中的任务队列通常可以设计为：

```cpp
std::queue<std::function<void()>>
```

将 `packaged_task` 包装后放入任务队列：

```cpp
auto task = std::make_shared<std::packaged_task<int()>>(
    [] {
        return 10 + 20;
    }
);

std::future<int> result = task->get_future();

taskQueue.push([task] {
    (*task)();
});
```

工作线程从任务队列中取出任务：

```cpp
std::function<void()> task = taskQueue.front();
taskQueue.pop();

task();
```

调用者通过：

```cpp
result.get();
```

获得任务结果。

这正是很多 C++ 线程池返回 `future` 的核心实现思路。

------

# 十三、常见注意事项

## 1. `future.get()` 只能调用一次

```cpp
future.get();
future.get(); // 错误
```

需要多次读取时，转换成：

```cpp
std::shared_future
```

------

## 2. 建议显式指定启动策略

不建议完全依赖默认策略：

```cpp
std::async(task);
```

更明确的写法：

```cpp
std::async(std::launch::async, task);
```

否则任务可能被延迟执行。

------

## 3. 必须保存 `async` 返回的 `future`

不推荐这样写：

```cpp
std::async(std::launch::async, task);
```

因为临时 `future` 立即析构时，某些情况下会等待异步任务结束，使代码表现得像同步调用。

建议：

```cpp
auto future =
    std::async(std::launch::async, task);
```

------

## 4. `promise` 只能设置一次结果

```cpp
promise.set_value(100);
promise.set_value(200); // 错误
```

同一个共享状态只能被满足一次。

------

## 5. `get_future()` 通常只能调用一次

```cpp
auto f1 = promise.get_future();
auto f2 = promise.get_future(); // 错误
```

------

## 6. 注意对象生命周期

```cpp
Calculator calculator;

auto future = std::async(
    std::launch::async,
    &Calculator::add,
    &calculator,
    10,
    20
);
```

必须保证在异步任务执行结束前，`calculator` 仍然存在。

------

## 7. 异步不代表一定更快

创建线程存在开销：

- 创建线程
- 上下文切换
- 任务调度
- 同步等待
- 缓存失效

对于非常小的任务：

```cpp
return a + b;
```

使用异步线程反而可能更慢。

异步更适合：

- 耗时计算
- 文件读取
- 网络请求
- 多个相互独立的任务
- 可以并行执行的计算

------

# 十四、综合示例

```cpp
#include <iostream>
#include <future>
#include <thread>
#include <chrono>
#include <vector>

int calculate(int id, int value)
{
    std::cout << "任务 " << id
              << " 开始，线程ID："
              << std::this_thread::get_id()
              << '\n';

    std::this_thread::sleep_for(
        std::chrono::seconds(1)
    );

    return value * value;
}

int main()
{
    std::vector<std::future<int>> futures;

    for (int i = 1; i <= 5; ++i)
    {
        futures.push_back(
            std::async(
                std::launch::async,
                calculate,
                i,
                i
            )
        );
    }

    std::cout << "主线程继续执行" << '\n';

    for (auto& future : futures)
    {
        try
        {
            std::cout << "结果："
                      << future.get()
                      << '\n';
        }
        catch (const std::exception& e)
        {
            std::cout << "异常："
                      << e.what()
                      << '\n';
        }
    }

    return 0;
}
```

这里：

```cpp
std::vector<std::future<int>>
```

用于保存多个异步任务的结果。

------

# 十五、核心知识总结

C++11 异步编程的核心关系是：

```text
                共享状态
              ┌─────────┐
生产端 ──────→│ 结果/异常 │──────→ 消费端
              └─────────┘
```

对应工具：

```text
生产端：
std::async
std::promise
std::packaged_task

消费端：
std::future
std::shared_future
```

最常用写法：

```cpp
std::future<int> result =
    std::async(std::launch::async, task);

int value = result.get();
```

最终可以记住以下几点：

1. `std::async` 用于启动异步任务。
2. `std::future` 用于获取未来结果。
3. `get()` 会等待任务完成，并且只能调用一次。
4. `wait()` 只等待，不获取结果。
5. `wait_for()` 可以限时等待。
6. `promise` 用于手动在线程之间传值。
7. `packaged_task` 用于封装可调用任务，适合任务队列和线程池。
8. `shared_future` 允许多个线程共享同一个结果。
9. 异步任务中的异常会在调用 `get()` 时重新抛出。
10. `std::launch::async` 和 `std::launch::deferred` 决定任务的执行方式。