# C++11 新特性复习笔记

> **目标：理解 C++11 为什么这样设计、什么时候使用，以及常见的易错点。示例默认省略 `std::` 时，已使用 `using namespace std;`。**

## 一、快速总览

| 模块 | 重点特性 | 解决的问题 |
| --- | --- | --- |
| **类型推导** | **`auto`、`decltype`** | **减少冗长类型书写，同时保留类型信息** |
| **初始化与常量** | **`{}` 初始化、成员默认值、`constexpr`** | **统一初始化语法，在编译期计算** |
| **面向对象** | **`override`、`final`、委托/继承构造** | **更安全地表达继承关系** |
| **函数式工具** | **Lambda、`std::function`、`std::bind`** | **将“可执行逻辑”作为对象传递** |
| **资源与性能** | **右值引用、移动语义、`std::move`、完美转发** | **避免不必要的深拷贝** |
| **安全与工具** | **`nullptr`、`static_assert`、`noexcept`** | **消除歧义并尽早暴露错误** |

---

## 二、基础语法改进

### 2.1 原始字符串字面量 `R"(...)"`

**原始字符串不会把 `\\`、`\n`、`\t` 当作转义字符，适合路径、正则、JSON、HTML 等含大量反斜杠或引号的文本。**

```cpp
#include <iostream>
#include <string>

int main() {
    std::string path = R"(C:\Users\lic\Desktop\test.txt)";
    std::string regex = R"(\d+\.\d+)";

    // 自定义分隔符，避免内容中恰好出现 )"
    std::string json = R"json(
{
  "name": "lic",
  "age": 22
}
)json";

    std::cout << path << '\n' << regex << '\n' << json;
}
```

语法为：`R"分隔符(内容)分隔符"`。分隔符可省略；若内容里会出现 `)"`，应使用例如 `json` 这样的自定义分隔符。

### 2.2 `long long` 与定宽整数

`long long` 至少 64 位；其范围可通过 `<climits>` 中的 `LLONG_MAX`、`LLONG_MIN` 查看。

```cpp
#include <climits>
#include <cstdint>

long long total = 9'000'000'000LL; // C++14 才支持数字分隔符，此处仅说明后缀
std::int64_t id = 9000000000LL;    // 需要精确位数时更推荐 int64_t
```

注意：上例中的单引号数字分隔符是 **C++14** 特性；在纯 C++11 中写成 `9000000000LL`。`long` 在不同平台位数可能不同，网络协议、文件格式等需要固定宽度时优先用 `<cstdint>` 的类型。

### 2.3 统一初始化 `{}`

花括号初始化可以用于内置类型、容器、数组、对象和动态对象，是 C++11 推荐的统一写法。

```cpp
#include <vector>
#include <map>
#include <string>

int x{10};
double pi{3.14};
int arr[]{1, 2, 3};
std::vector<int> v{1, 2, 3};
std::map<int, std::string> m{{1, "one"}, {2, "two"}};

struct Person 
{
    int age;
    std::string name;
};
Person p{22, "lic"};
```

它的一个重要价值是禁止**窄化转换**：

```cpp
int a = 3.14;  // 可以编译，但丢失小数部分
// int b{3.14}; // 编译报错：窄化转换
```

### 2.4 类内成员默认初始化

把常见默认值写在成员声明处；构造函数只覆盖需要变化的成员，减少漏初始化风险。

```cpp
#include <string>

class User {
public:
    User() = default;
    explicit User(std::string name) : name_(std::move(name)) {}

private:
    int id_{0};
    bool online_{false};
    std::string name_{"anonymous"};
};
```

构造函数初始化列表中的值优先级更高：`User(std::string n) : name_(std::move(n)) {}` 会覆盖 `name_` 的默认值。

对于 `static const int` 这类整型常量，C++11 可在类内给出值；但若发生 ODR-use（例如取地址），通常仍需要类外定义。C++17 的 `inline static` 才彻底简化此问题。

### 2.5 模板右尖括号

C++11 起，嵌套模板可直接写 `>>`，无需写成 `> >`。

```cpp
std::vector<std::vector<int>> matrix;
std::map<int, std::vector<std::string>> table;
```

---

## 三、类型推导与遍历

### 3.1 `auto`

`auto` 根据初始化表达式推导变量类型，**必须初始化**。它特别适合迭代器、复杂模板类型和 Lambda。

```cpp
#include <map>
#include <string>

std::map<int, std::string> dict{{1, "one"}, {2, "two"}};
auto it = dict.begin();  // std::map<int, std::string>::iterator
auto n = 42;             // int
auto pi = 3.14;          // double
```

`auto` 的默认规则类似模板按值传参：会忽略顶层 `const` 和引用。

```cpp
const int x = 10;
auto a = x;        // int
const auto b = x;  // const int

int y = 20;
int& ref = y;
auto c = ref;      // int，得到副本
auto& d = ref;     // int&，保留引用
const auto& e = x; // const int&，只读引用
```

实战建议：

- 需要修改原对象：用 `auto&`。
- 只读且避免大对象拷贝：用 `const auto&`。
- 只想得到独立副本：用 `auto`。
- 不要为了“短”而滥用；若类型本身就是 `int`、`std::string`，直接写类型往往更清楚。

### 3.2 `decltype`

`decltype(expr)` 推导的是表达式 `expr` 的类型，**不执行** `expr`。它适合泛型编程和推导函数返回类型。

```cpp
int x = 10;
const int& rx = x;

decltype(x) a = 1;    // int
decltype(rx) b = x;   // const int&
```

最容易混淆的是变量加括号：

```cpp
int n = 0;
decltype(n)  a = 1;   // int：未加括号的变量，得到声明类型
decltype((n)) b = n;  // int&：括号后是左值表达式

const int cn = 0;
decltype((cn)) c = cn; // const int&
```

常用的尾置返回类型写法：

```cpp
template<class T, class U>
auto add(const T& lhs, const U& rhs) -> decltype(lhs + rhs) {
    return lhs + rhs;
}
```

> C++14 以后可直接写 `auto add(...) { return lhs + rhs; }`；C++11 中返回类型不能依赖尚未出现在作用域中的形参，因此常使用尾置返回类型。

### 3.3 范围 `for`

范围 `for` 本质上由编译器展开为 `begin()` / `end()` 迭代，适合遍历整个可迭代对象。

```cpp
#include <vector>
#include <map>
#include <string>

std::vector<int> v{1, 2, 3};

for (int value : v) {          // 拷贝元素
    // value 的修改不会影响 v
}
for (int& value : v) {         // 引用元素，可修改 v
    value *= 2;
}
for (const auto& value : v) {  // 推荐的只读大对象遍历方式
    // 读取 value
}

std::map<int, std::string> m{{1, "one"}};
for (const auto& kv : m) {
    // kv.first 是 const，map 的 key 不允许修改
}
```

不要返回或保存循环变量的引用到循环外；容器扩容、元素删除或循环结束后都可能使其失效。

### 3.4 `nullptr`

`nullptr` 是专用的空指针字面量，类型为 `std::nullptr_t`，应替代 `0` 和 `NULL`。

```cpp
void func(int);
void func(char*);

func(0);        // 调用 func(int)
func(nullptr);  // 调用 func(char*)
```

原因：在 C++ 中，`NULL` 常被定义为 `0` 或 `0L`，并不是一个真正“只表示空指针”的类型；重载时会产生歧义或选错函数。

---

## 四、编译期计算与检查

### 4.1 `static_assert`

`assert` 在**运行期**检查；`static_assert` 在**编译期**检查。编译期能确定的约束优先使用 `static_assert`。

```cpp
#include <type_traits>

template<class T>
class Buffer {
    static_assert(std::is_trivially_copyable<T>::value,
                  "Buffer requires trivially copyable T");
};

static_assert(sizeof(void*) == 8, "This program requires a 64-bit platform");
```

```cpp
#include <cassert>

int createArray(int size) {
    assert(size > 0); // 输入运行时才知道，只能运行期检查
    return size;
}
```

### 4.2 `constexpr`

`const` 表示“不能通过这个名字修改”；`constexpr` 表示“要求它可在编译期求值”。`constexpr` 对象隐含 `const`。

```cpp
constexpr int square(int x) {
    return x * x; // C++11 的 constexpr 函数体限制较严格，通常只有一条 return
}

constexpr int n = square(5);
int arr[n]; // 数组长度可在编译期确定

int runtime = 3;
const int a = runtime;      // 只读，但通常不是常量表达式
constexpr int b = square(3); // 编译期常量
```

注意：`constexpr` 函数也可在运行期调用，例如 `square(runtime)`；是否在编译期计算取决于实参和使用场景。

---

## 五、继承与构造改进

### 5.1 `override`

`override` 让编译器检查该函数是否真的覆盖了基类虚函数。它不是运行时机制，而是非常重要的编译期防错工具。

```cpp
class Base {
public:
    virtual void print() const {}
    virtual ~Base() = default;
};

class Derived : public Base {
public:
    void print() const override {} // 签名不匹配会立即编译报错
};
```

常见错误包括漏写 `const`、参数类型不同、成员函数限定符不同。只要是重写，建议一律加 `override`。

### 5.2 `final`

`final` 有两种用法：

```cpp
class Base {
public:
    virtual void f() {}
};

class Derived final : public Base { // Derived 不能再被继承
public:
    void f() final {}               // f 不能再被进一步重写
};
```

`final` 适合明确禁止扩展的类型或接口终点；不要仅为“优化”而随意使用，它会限制后续设计。

### 5.3 委托构造函数

一个构造函数可委托给同类的另一个构造函数，避免重复初始化逻辑。

```cpp
#include <string>

class User {
public:
    User() : User(0, "anonymous") {}          // 委托构造
    explicit User(std::string name) : User(0, std::move(name)) {}
    User(int id, std::string name) : id_(id), name_(std::move(name)) {}

private:
    int id_;
    std::string name_;
};
```

一个构造函数只能委托给一个目标构造函数；委托目标完成后，再执行当前构造函数的函数体。

### 5.4 继承构造函数

`using Base::Base;` 可以把基类构造函数引入派生类，适合派生类不需要新增初始化逻辑的场景。

```cpp
class Base {
public:
    explicit Base(int x) : x_(x) {}
protected:
    int x_;
};

class Derived : public Base {
public:
    using Base::Base;
};

Derived d{10};
```

如果派生类有自己的成员，它们仍按默认成员初始化规则初始化；无法初始化的成员会使相应构造函数被删除。

---

## 六、类型别名 `using`

`using` 可替代 `typedef`，并且可以定义别名模板，因此在现代 C++ 中更推荐。

```cpp
using Callback = void (*)(int, const std::string&);

template<class T>
using IntMap = std::map<int, T>;

IntMap<std::string> names;
```

`typedef` 不能直接创建别名模板：

```cpp
// template<class T>
// typedef std::map<int, T> IntMap; // 错误
```

---

## 七、Lambda 表达式

### 7.1 基本语法

```cpp
[capture](parameters) mutable -> return_type { body };
```

其中参数列表和返回类型在可推导时可省略。

```cpp
auto add = [](int x, int y) { return x + y; };
std::cout << add(2, 3); // 5
```

Lambda 本质上是编译器生成的匿名仿函数对象，常用于算法回调和异步任务。

### 7.2 捕获方式

```cpp
int x = 10;
int y = 20;

auto byValue = [x] { return x; }; // 按值捕获：保存创建 Lambda 时 x 的副本
auto byRef   = [&x] { ++x; };      // 按引用捕获：操作外部 x
auto allVal  = [=] { return x + y; };
auto allRef  = [&] { ++x; ++y; };
auto mixed   = [x, &y] { return x + ++y; };
```

按值捕获的成员在默认情况下不可修改，因为 Lambda 的 `operator()` 默认是 `const`。需要修改其内部副本时加 `mutable`：

```cpp
auto count = [x]() mutable { return ++x; };
```

`mutable` 改的是 Lambda 内部保存的副本，不会改外部 `x`。按引用捕获要格外注意生命周期：异步回调中捕获局部变量引用很容易悬空。

### 7.3 与 STL 算法结合

```cpp
#include <algorithm>
#include <vector>

std::vector<int> v{1, 2, 3, 4, 5, 6};
int threshold = 3;

auto it = std::find_if(v.begin(), v.end(),
                       [threshold](int value) {
                           return value > threshold;
                       });
```

---

## 八、可调用对象、`std::function` 与 `std::bind`

### 8.1 什么是可调用对象

以下都可以像函数一样调用：普通函数、函数指针、Lambda、重载 `operator()` 的仿函数、成员函数指针（需配合对象）。

```cpp
void print(int x) {}

struct Printer {
    void operator()(int x) const {}
};

auto lambda = [](int x) {};
Printer functor;

print(1);
functor(2);
lambda(3);
```

### 8.2 `std::function`：统一的函数包装器

`std::function<R(Args...)>` 可以保存签名兼容的多种可调用对象，是回调接口的常用类型。

```cpp
#include <functional>
#include <iostream>

int add(int a, int b) { return a + b; }

std::function<int(int, int)> f = add;
f = [](int a, int b) { return a * b; };
std::cout << f(2, 3); // 6
```

注意：`std::function` 有类型擦除开销，性能极端敏感的模板内部可直接接收可调用对象模板参数；业务层回调、事件分发中则很实用。调用空的 `std::function` 会抛出 `std::bad_function_call`，可先判断 `if (f)`。

### 8.3 `std::bind`：绑定参数并重排调用

```cpp
#include <functional>
#include <iostream>

int add(int a, int b) { return a + b; }
using namespace std::placeholders;

auto f1 = std::bind(add, 10, 20); // 无参，等价于 add(10, 20)
auto f2 = std::bind(add, _1, 10); // 一个参数，等价于 add(arg1, 10)
auto f3 = std::bind(add, _2, _1); // 两个参数，交换参数顺序

std::cout << f1() << ' ' << f2(5) << ' ' << f3(3, 8); // 30 15 11
```

绑定成员函数时，第一个参数必须提供对象（对象地址、引用包装器或智能指针）：

```cpp
class Worker {
public:
    void run(int id) { std::cout << id << '\n'; }
};

Worker worker;
auto task = std::bind(&Worker::run, &worker, std::placeholders::_1);
task(100);
```

现代代码中，简单场景通常更推荐 Lambda，可读性更高：

```cpp
auto task2 = [&worker](int id) { worker.run(id); };
```

---

## 九、右值引用与移动语义

### 9.1 左值、右值与引用

- **左值（lvalue）**：有稳定身份、可取地址的表达式，例如变量 `x`。
- **右值（rvalue）**：通常是临时值，例如 `42`、`std::string("hi")`。
- `T&` 绑定左值；`T&&` 可绑定右值。

```cpp
int x = 10;
int& lref = x;   // 左值引用
int&& rref = 20; // 右值引用，临时对象生命周期延长到 rref 的作用域结束
```

**重要：有名字的右值引用变量本身是左值。**

```cpp
void consume(const std::string&); // 拷贝/只读版本
void consume(std::string&&);      // 可移动版本

std::string s = "hello";
consume(std::move(s)); // std::move 将表达式显式转换为右值
```

### 9.2 移动构造与移动赋值

移动不是“移动对象本身”，而是转移它管理的资源所有权。典型对象应同时遵守资源管理规则：有析构、拷贝、移动时，通常需要完整考虑 Rule of Five。

```cpp
#include <utility>

class Buffer {
public:
    explicit Buffer(std::size_t size)
        : size_(size), data_(new int[size]) {}

    ~Buffer() { delete[] data_; }

    Buffer(const Buffer& other)                 // 深拷贝
        : size_(other.size_), data_(new int[other.size_]) {
        std::copy(other.data_, other.data_ + size_, data_);
    }

    Buffer(Buffer&& other) noexcept             // 转移资源
        : size_(other.size_), data_(other.data_) {
        other.size_ = 0;
        other.data_ = nullptr;
    }

    Buffer& operator=(Buffer other) {           // copy-and-swap，可同时处理拷贝/移动
        swap(other);
        return *this;
    }

    void swap(Buffer& other) noexcept {
        std::swap(size_, other.size_);
        std::swap(data_, other.data_);
    }

private:
    std::size_t size_{0};
    int* data_{nullptr};
};
```

容器扩容时会优先使用 `noexcept` 的移动构造；若移动可能抛异常，`std::vector` 为保证异常安全性可能改用拷贝构造。因此移动构造/移动赋值在确实不抛异常时应标为 `noexcept`。

### 9.3 `std::move` 不是移动操作

`std::move(x)` 只做一次类型转换：把 `x` 转成可被当作右值处理的表达式。真正是否移动，由后续调用的函数/构造函数决定。

```cpp
std::string source = "large text";
std::string target = std::move(source); // target 的移动构造负责转移资源

// source 仍然有效，但内容处于“有效但未指定”状态；除赋值、析构等操作外，不应依赖其原值。
```

不要对仍需保留原值的对象使用 `std::move`。

### 9.4 完美转发 `std::forward`

模板中的 `T&&` 在类型推导时是**转发引用**（旧称万能引用）：实参是左值时 `T` 推导为 `U&`，实参是右值时 `T` 推导为 `U`。但参数 `arg` 有名字，表达式永远是左值；需要 `std::forward<T>(arg)` 保留原始值类别。

```cpp
#include <utility>

void process(const std::string&) { /* 左值版本 */ }
void process(std::string&&)      { /* 右值版本 */ }

template<class T>
void relay(T&& arg) {
    process(std::forward<T>(arg)); // 左值仍转发为左值，右值仍转发为右值
}
```

对比：`std::move(arg)` 会无条件把 `arg` 转为右值，不能保留调用者传入的是左值还是右值。

---

## 十、异常说明 `noexcept`

`noexcept` 表示函数**不会抛出异常**。若异常从 `noexcept` 函数逃逸，程序会调用 `std::terminate()`，而不是正常继续传播异常。

```cpp
void cleanup() noexcept {
    // 这里不应让异常逃出函数
}

template<class T>
void swapValue(T& a, T& b) noexcept(noexcept(T(std::move(a)))) {
    T temp(std::move(a));
    a = std::move(b);
    b = std::move(temp);
}
```

不要把可能抛异常的业务函数随意标记为 `noexcept`；最常见的应用是析构函数、`swap`、移动构造和移动赋值（前提是它们确实不会抛异常）。旧式动态异常规范 `throw()`、`throw(int)` 已被废弃，不建议使用。

---

## 十一、字符串与数值转换

### 11.1 数值转字符串

```cpp
#include <string>

std::string a = std::to_string(123);
std::string b = std::to_string(3.14);
```

`std::to_string` 支持整型和浮点型。浮点输出格式固定性较弱，展示或序列化格式有严格要求时可使用流、`std::ostringstream`（C++11）或 C++17 的 `std::to_chars`。

### 11.2 字符串转数值

```cpp
int i = std::stoi("123");
long long ll = std::stoll("9000000000");
double d = std::stod("3.14159");

std::size_t pos = 0;
int hex = std::stoi("0xff rest", &pos, 0); // base=0 自动识别 0x，pos 指向转换结束处
```

转换失败会抛 `std::invalid_argument`；数值越界会抛 `std::out_of_range`。处理外部输入时应捕获异常或先做校验。

---

## 十二、面试高频易错点

| 问题 | 正确结论 |
| --- | --- |
| `auto` 会保留 `const` 和引用吗？ | 默认不保留顶层 `const` 与引用；需要时明确写 `const auto&`、`auto&`。 |
| `decltype(a)` 与 `decltype((a))` 区别？ | 前者变量名得到声明类型；后者按表达式值类别推导，左值通常得到 `T&`。 |
| `nullptr` 与 `NULL`？ | `nullptr` 是专用空指针类型；`NULL` 通常是整数 `0`。优先 `nullptr`。 |
| `const` 与 `constexpr`？ | `const` 表示不可修改；`constexpr` 要求可用于编译期常量表达式。 |
| `override` 与 `final`？ | `override` 检查是否正确重写；`final` 禁止继续继承或重写。 |
| `std::move` 做了什么？ | 只把表达式转换为右值；实际资源转移由后续移动构造/赋值执行。 |
| 为什么 `std::forward` 必须写模板参数？ | 用 `T` 恢复实参原本的左/右值类别；参数变量本身永远是左值。 |
| `noexcept` 函数抛异常会怎样？ | 异常逃逸时调用 `std::terminate()`。 |

---

## 十三、建议的复习顺序

1. 先掌握 `{}`、`auto`、范围 `for`、`nullptr`、`using`，它们最常出现在日常代码中。
2. 再掌握 `decltype`、`constexpr`、`static_assert`，建立“编译期类型/计算”的思维。
3. 然后重点攻克 Lambda、`function`、`bind`，理解回调和 Reactor 中的事件分发写法。
4. 最后反复练习右值引用、移动语义、`move` 与 `forward`；这是 C++11 面试和容器性能的核心。

## 十四、最小编译命令

```bash
g++ -std=c++11 -Wall -Wextra -Wpedantic main.cpp -o main
```

建议始终打开警告；许多 `override`、隐式转换和未初始化问题能在编译期提前发现。
