# 设计模式

## 1.单例模式

**这是饿汉模式:  已经创建好了，什么时候需要用，直接拿去就行的。**

```c++
#include <iostream>
using namespace std;

class TaskQueue
{
public:
	TaskQueue(const TaskQueue& t) = delete;
	TaskQueue& operator=(const TaskQueue& t) = delete;
		
	static TaskQueue* getInstance()
	{
		return taskQueue;
	}

	void print()
	{
		cout << "我是一个单例对象" << endl;
	}

private:
	TaskQueue() = default;
	static TaskQueue* taskQueue;

	//TaskQueue(const TaskQueue& t) = default
	//TaskQueue& operator=(const TaskQueue& t) = default;
};

// 这是饿汉模式
TaskQueue* TaskQueue::taskQueue = new TaskQueue();

int main()
{
	TaskQueue* t1 = TaskQueue::getInstance();
	TaskQueue* t2 = TaskQueue::getInstance();
	t1->print();

	cout << t1 << endl;
	cout << t2 << endl;
	
	return 0;
}
```

**1. 拷贝构造和赋值构造： 删除或者设置为私有**

**2.默认构造函数设置为私有的。**

**3.静态的成员属性存储对象， 静态成员函数唯一方法获取的。**



**懒汉模式：用的时候才给你的。**

**懒汉模式：需要解决线程安全问题的。**

**双重检查锁**

```c++
#include <iostream>
#include <mutex>
using namespace std;

class TaskQueue
{
public:
	TaskQueue(const TaskQueue& t) = delete;
	TaskQueue& operator=(const TaskQueue& t) = delete;
		
	static TaskQueue* getInstance()
	{	
		if (_taskQueue == nullptr) // 第二次abc线程
		{
			_mutex.lock();         // 第一次 xyz线程
			if (_taskQueue == nullptr)
			{
				// 1.申请内存， 2.创建一个对象然后存储， 3，存储指针。
				// 
				// 1.申请内存， 2.存储指针， 3，创建一个对象然后存储。
				// 问题：第二步就返回了的。
				_taskQueue = new TaskQueue(); 
			}

			_mutex.unlock();
		}

		return _taskQueue;
	}

	void print()
	{
		cout << "我是一个单例对象" << endl;
	}

private:
	TaskQueue() = default;
	static TaskQueue* _taskQueue;
	static mutex _mutex;

	//TaskQueue(const TaskQueue& t) = default
	//TaskQueue& operator=(const TaskQueue& t) = default;
};

TaskQueue* TaskQueue::_taskQueue = nullptr;
mutex TaskQueue::_mutex; // 声明一下

int main()
{
	TaskQueue* t1 = TaskQueue::getInstance();
	TaskQueue* t2 = TaskQueue::getInstance();
	t1->print();

	cout << t1 << endl;
	cout << t2 << endl;
	
	return 0;
}
```

**第一次：线程任然是是串行的。**

**第二次：线程都不需要加锁，解锁。**



```c++
#include <iostream>
#include <mutex>
using namespace std;

class TaskQueue
{
public:
	TaskQueue(const TaskQueue& t) = delete;
	TaskQueue& operator=(const TaskQueue& t) = delete;
		
	static TaskQueue* getInstance()
	{	
		TaskQueue* task = _taskQueue.load();
		if (task == nullptr) 
		{
			_mutex.lock();         
			task = _taskQueue.load();
			if (task == nullptr)
			{
				task = new TaskQueue; 
				_taskQueue.store(task);
			}

			_mutex.unlock();
		}

		return task;
	}

	void print()
	{
		cout << "我是一个单例对象" << endl;
	}

private:
	TaskQueue() = default;
	static mutex _mutex;
	static atomic<TaskQueue*> _taskQueue;
};

atomic<TaskQueue*> TaskQueue::_taskQueue; // 声明一下
mutex TaskQueue::_mutex;                  // 声明一下

int main()
{
	TaskQueue* t1 = TaskQueue::getInstance();
	TaskQueue* t2 = TaskQueue::getInstance();
	t1->print();

	cout << t1 << endl;
	cout << t2 << endl;
	
	return 0;
}
```



**使用原子变量解决**

```c++
#include <iostream>
#include <mutex>
using namespace std;

class TaskQueue
{
public:
	TaskQueue(const TaskQueue& t) = delete;
	TaskQueue& operator=(const TaskQueue& t) = delete;
		
	static TaskQueue* getInstance()
	{	
		TaskQueue* task = _taskQueue.load();
		if (task == nullptr) 
		{
			_mutex.lock();         
			task = _taskQueue.load();
			if (task == nullptr)
			{
				task = new TaskQueue; 
				_taskQueue.store(task);
			}

			_mutex.unlock();
		}

		return task;
	}

	void print()
	{
		cout << "我是一个单例对象" << endl;
	}

private:
	TaskQueue() = default;
	static mutex _mutex;
	static atomic<TaskQueue*> _taskQueue;
};

atomic<TaskQueue*> TaskQueue::_taskQueue; // 声明一下
mutex TaskQueue::_mutex;                  // 声明一下

int main()
{
	TaskQueue* t1 = TaskQueue::getInstance();
	TaskQueue* t2 = TaskQueue::getInstance();
	t1->print();

	cout << t1 << endl;
	cout << t2 << endl;
	
	return 0;
}
```



**静态局部对象。**

```c++
#include <iostream>
#include <mutex>
using namespace std;

class TaskQueue
{
public:
	TaskQueue(const TaskQueue& t) = delete;
	TaskQueue& operator=(const TaskQueue& t) = delete;
		
	static TaskQueue* getInstance()
	{	
		static TaskQueue task; // c++11 支持。
		return &task;
	}

	void print()
	{
		cout << "我是一个单例对象" << endl;
	}

private:
	TaskQueue() = default;
};

int main()
{
	TaskQueue* t1 = TaskQueue::getInstance();
	TaskQueue* t2 = TaskQueue::getInstance();
	t1->print();

	cout << t1 << endl;
	cout << t2 << endl;
	
	return 0;
}
```



**案例**

```c++
#include <iostream>
#include <mutex>
#include <queue>
#include <thread>
using namespace std;

class TaskQueue
{
public:
	TaskQueue(const TaskQueue& t) = delete;
	TaskQueue& operator=(const TaskQueue& t) = delete;
		
	static TaskQueue* getInstance()
	{	
		return _taskQueue;
	}

	// 1.判断是否为空的
	bool empty()
	{
		lock_guard<mutex> locker(_mutex);
		return _data.empty();
	}

	// 2.添加任务
	void addtask(int x)
	{
		lock_guard<mutex> locker(_mutex);
		_data.push(x);
	}

	// 3.删除任务
	bool poptask()
	{
		lock_guard<mutex> locker(_mutex);
		if (_data.empty())
		{
			return false;
		}

		_data.pop();

		return true;
	}

	// 4.取任务
	int  taketask()
	{
		lock_guard<mutex> locker(_mutex);
		if (_data.empty())
		{
			return -1;
		}

		return _data.front();
	}

private:
	TaskQueue() = default;
	static TaskQueue* _taskQueue;

	// 任务队列
	queue<int> _data;
	mutex _mutex;
};

TaskQueue* TaskQueue::_taskQueue = new TaskQueue;

int main()
{
	TaskQueue* task = TaskQueue::getInstance();

	// 生产者
	thread t1([=]()
		{
			for (int i = 0; i < 10; ++i)
			{
				task->addtask(i + 10);
				cout << "push data:" << i + 10 << "thread id :" << this_thread::get_id() << endl;
				this_thread::sleep_for(chrono::microseconds(500));
			}
		});

	// 消费者
	thread t2([=]()
		{
			this_thread::sleep_for(chrono::microseconds(100));
			while (!task->empty())
			{
				int num = task->taketask();
				cout << "take data:" << num << "thread id :" << this_thread::get_id() << endl;
				task->poptask();
				this_thread::sleep_for(chrono::microseconds(1000));
			}
		});


	t1.join();
	t2.join();

	return 0;
}
```



## 2.工程模式

**2.1 简单工厂模型**

```c++
#include <iostream>

// 强类型枚举
enum class Type:char{Sheep, Lion, Bat};

using namespace std;

// 1.父类：纯虚函数
class AbstractSmile
{
public:
	virtual void transform() = 0;
	virtual void ability() = 0;
	virtual ~AbstractSmile() {}; // 虚析构
};

class SheepSmile : public AbstractSmile
{
public:
	virtual void transform() override
	{
		cout << "变成山羊" << endl;
	}

	virtual void ability() override
	{
		cout << "ability变成山羊" << endl;
	}

	virtual ~SheepSmile() override
	{

	}; // 虚析构
};

class LionSmile : public AbstractSmile
{
public:
	virtual void transform() override
	{
		cout << "变成狮子" << endl;
	}

	virtual void ability() override
	{
		cout << "ability变成狮子" << endl;
	}

	virtual ~LionSmile() override
	{

	}; // 虚析构
};

class BatSmile : public AbstractSmile
{
public:
	virtual void transform() override
	{
		cout << "变成蝙蝠" << endl;
	}

	virtual void ability() override
	{
		cout << "ability变成蝙蝠" << endl;
	}

	virtual ~BatSmile() override
	{

	}; // 虚析构
};

class SmileFactory
{
public:
	AbstractSmile* create(Type type)
	{
		AbstractSmile* ptr = nullptr;

		switch (type)
		{
		case Type::Sheep:
			ptr = new SheepSmile;
			break;
		case Type::Lion:
			ptr = new LionSmile;
			break;
		case Type::Bat:
			ptr = new BatSmile;
			break;
		}

		return ptr;
	}
};

int main()
{
	SmileFactory* factory = new SmileFactory;
	AbstractSmile* obj = factory->create(Type::Lion);

	obj->transform();
	obj->ability();

	return 0;
}
```





**2.2 抽象工程**







**2.3 抽象工厂**

































