c11 线程

# C++11 线程库总结

C++11 在标准库中正式加入了多线程支持，主要用于：

- 创建和管理线程
- 保护共享资源
- 实现线程间同步
- 实现线程间数据传递
- 执行异步任务
- 实现无锁原子操作

常用头文件：

```cpp
#include <thread>
#include <mutex>
#include <condition_variable>
#include <future>
#include <atomic>
#include <chrono>
```

------

# 一、`std::thread`：创建和管理线程

## 1. 创建线程

```cpp
#include <iostream>
#include <thread>

void work(int x)
{
    std::cout << "x = " << x << '\n';
}

int main()
{
    std::thread t(work, 10);

    t.join();

    return 0;
}
```

构造线程对象后，新线程会立即开始执行。

线程入口可以是：

- 普通函数
- Lambda 表达式
- 仿函数
- 类的成员函数
- `std::bind` 产生的可调用对象

------

## 2. 使用 Lambda 创建线程

```cpp
std::thread t([]()
{
    std::cout << "子线程运行\n";
});

t.join();
```

------

## 3. 调用成员函数

```cpp
class Worker
{
public:
    void run(int x)
    {
        std::cout << "x = " << x << '\n';
    }
};

int main()
{
    Worker worker;

    std::thread t(&Worker::run, &worker, 100);
    t.join();
}
```

参数含义：

```cpp
std::thread t(&Worker::run, &worker, 100);
```

分别是：

```text
成员函数地址
调用该函数的对象地址
传递给成员函数的参数
```

------

# 二、`join()` 和 `detach()`

线程创建以后，必须确定线程的结束方式。

## 1. `join()`

等待子线程执行结束：

```cpp
std::thread t(work, 10);
t.join();
```

调用 `join()` 后，主线程会阻塞，直到子线程运行结束。

执行关系：

```text
主线程创建子线程
        ↓
主线程调用 join()
        ↓
主线程等待
        ↓
子线程执行结束
        ↓
主线程继续执行
```

------

## 2. `detach()`

让子线程与线程对象分离：

```cpp
std::thread t(work, 10);
t.detach();
```

分离后：

- 主线程不再等待子线程
- 子线程独立运行
- 无法再次 `join()`
- 无法可靠获取线程执行结果

使用 `detach()` 时要特别注意对象生命周期。

错误示例：

```cpp
void test()
{
    int x = 10;

    std::thread t([&x]()
    {
        std::cout << x << '\n';
    });

    t.detach();
}
```

`test()` 返回后，局部变量 `x` 被销毁，但子线程可能仍然访问它，会产生未定义行为。

通常优先使用：

```cpp
join()
```

而不是 `detach()`。

------

## 3. `joinable()`

判断线程是否可以调用 `join()` 或 `detach()`：

```cpp
if (t.joinable())
{
    t.join();
}
```

以下线程不可连接：

- 默认构造的线程
- 已经调用 `join()` 的线程
- 已经调用 `detach()` 的线程
- 被移动后的原线程对象

------

## 4. 线程对象析构的注意事项

如果 `std::thread` 对象析构时仍然是 `joinable()` 状态，程序会调用：

```cpp
std::terminate();
```

错误示例：

```cpp
int main()
{
    std::thread t(work, 10);

    // 没有 join，也没有 detach
}
```

因此必须保证：

```cpp
if (t.joinable())
{
    t.join();
}
```

------

# 三、线程参数传递

## 1. 默认按值传递

```cpp
void func(int x)
{
    x = 100;
}

int main()
{
    int value = 10;

    std::thread t(func, value);
    t.join();

    std::cout << value << '\n'; // 10
}
```

线程内部获得的是参数副本。

------

## 2. 使用 `std::ref` 传递引用

```cpp
#include <functional>

void func(int& x)
{
    x = 100;
}

int main()
{
    int value = 10;

    std::thread t(func, std::ref(value));
    t.join();

    std::cout << value << '\n'; // 100
}
```

仅仅写：

```cpp
std::thread t(func, value);
```

不会按照引用传递，必须使用：

```cpp
std::ref(value)
```

传递常量引用可以使用：

```cpp
std::cref(value)
```

------

## 3. 线程对象只能移动，不能复制

```cpp
std::thread t1(work, 10);

// 错误：thread 不支持复制
// std::thread t2 = t1;

// 正确：转移线程所有权
std::thread t2 = std::move(t1);

t2.join();
```

移动后：

```cpp
t1.joinable() == false
t2.joinable() == true
```

------

# 四、`std::this_thread`：当前线程操作

`std::this_thread` 是一个命名空间。

## 1. 获取当前线程 ID

```cpp
std::cout << std::this_thread::get_id() << '\n';
```

------

## 2. 线程休眠一段时间

```cpp
std::this_thread::sleep_for(std::chrono::seconds(2));
```

也可以使用毫秒：

```cpp
std::this_thread::sleep_for(std::chrono::milliseconds(500));
```

------

## 3. 休眠到某个时间点

```cpp
auto timePoint =
    std::chrono::steady_clock::now() +
    std::chrono::seconds(2);

std::this_thread::sleep_until(timePoint);
```

------

## 4. 主动让出 CPU

```cpp
std::this_thread::yield();
```

表示当前线程愿意暂时让出 CPU，由操作系统调度其他线程。

但这只是一个调度提示，并不保证其他线程一定立即运行。

------

# 五、数据竞争

多个线程同时访问同一个变量，并且至少有一个线程执行写操作，如果没有同步机制，就会产生数据竞争。

```cpp
int count = 0;

void add()
{
    for (int i = 0; i < 100000; ++i)
    {
        ++count;
    }
}
```

多个线程执行 `add()` 时，最终结果不一定正确。

因为：

```cpp
++count;
```

不是一个不可分割的操作，大致包含：

```text
读取 count
加 1
写回 count
```

多个线程可能交叉执行。

数据竞争会导致：

- 计算结果错误
- 程序崩溃
- 内存损坏
- 未定义行为

解决方式主要有：

- `std::mutex`
- `std::atomic`
- 消息传递
- 避免共享数据

------

# 六、`std::mutex`：互斥锁

互斥锁用于保护共享资源。

```cpp
#include <mutex>

std::mutex mtx;
int count = 0;

void add()
{
    for (int i = 0; i < 100000; ++i)
    {
        mtx.lock();
        ++count;
        mtx.unlock();
    }
}
```

在同一时刻，只有一个线程能够成功持有互斥锁。

------

## 1. 不建议手动 `lock()` 和 `unlock()`

```cpp
mtx.lock();

++count;

mtx.unlock();
```

如果中间发生异常：

```cpp
mtx.lock();

throw std::runtime_error("error");

mtx.unlock();
```

那么 `unlock()` 无法执行，锁永远不会被释放。

因此推荐使用 RAII 锁管理器。

------

# 七、`std::lock_guard`

`std::lock_guard` 是最简单的 RAII 锁管理器。

```cpp
void add()
{
    std::lock_guard<std::mutex> locker(mtx);
    ++count;
}
```

执行过程：

```text
创建 lock_guard
    ↓
自动调用 mutex.lock()
    ↓
执行临界区代码
    ↓
离开作用域
    ↓
自动调用 mutex.unlock()
```

优点：

- 写法简单
- 自动释放锁
- 异常安全
- 不容易遗漏 `unlock()`

缺点：

- 不能手动提前解锁
- 不支持条件变量等待
- 功能比 `unique_lock` 少

------

# 八、`std::unique_lock`

`std::unique_lock` 也是 RAII 锁管理器，但功能更加灵活。

```cpp
std::unique_lock<std::mutex> locker(mtx);
```

支持：

```cpp
locker.lock();
locker.unlock();
locker.owns_lock();
```

示例：

```cpp
void func()
{
    std::unique_lock<std::mutex> locker(mtx);

    ++count;

    locker.unlock();

    // 后面的代码不再持有锁
}
```

------

## `lock_guard` 和 `unique_lock` 的区别

| 特性           | `lock_guard` | `unique_lock`        |
| -------------- | ------------ | -------------------- |
| 自动加锁和解锁 | 支持         | 支持                 |
| 手动解锁       | 不支持       | 支持                 |
| 手动重新加锁   | 不支持       | 支持                 |
| 条件变量       | 不适合       | 必须使用             |
| 性能开销       | 较小         | 略大                 |
| 使用场景       | 简单临界区   | 条件变量、复杂锁控制 |

一般原则：

```text
普通加锁：lock_guard
条件变量：unique_lock
复杂锁管理：unique_lock
```

------

# 九、C++11 中常见互斥锁类型

## 1. `std::mutex`

普通互斥锁：

```cpp
std::mutex mtx;
```

特点：

- 同一个线程不能重复加锁
- 重复加锁可能导致死锁

------

## 2. `std::recursive_mutex`

递归互斥锁：

```cpp
std::recursive_mutex mtx;
```

允许同一个线程多次加锁，但加锁和解锁次数必须对应。

```cpp
mtx.lock();
mtx.lock();

mtx.unlock();
mtx.unlock();
```

通常不建议滥用递归锁，因为它可能掩盖代码设计问题。

------

## 3. `std::timed_mutex`

支持超时加锁：

```cpp
std::timed_mutex mtx;

if (mtx.try_lock_for(std::chrono::seconds(1)))
{
    // 成功获得锁
    mtx.unlock();
}
else
{
    // 一秒内没有获得锁
}
```

常用接口：

```cpp
try_lock()
try_lock_for()
try_lock_until()
```

------

## 4. `std::recursive_timed_mutex`

同时支持：

- 递归加锁
- 超时加锁

```cpp
std::recursive_timed_mutex mtx;
```

------

# 十、锁的不同构造策略

`unique_lock` 支持不同的锁策略。

## 1. 立即加锁

```cpp
std::unique_lock<std::mutex> lock(mtx);
```

------

## 2. 延迟加锁：`defer_lock`

```cpp
std::unique_lock<std::mutex> lock(mtx, std::defer_lock);

// 此时没有加锁
lock.lock();
```

------

## 3. 尝试加锁：`try_to_lock`

```cpp
std::unique_lock<std::mutex> lock(mtx, std::try_to_lock);

if (lock.owns_lock())
{
    // 获得锁
}
```

------

## 4. 接管已经获得的锁：`adopt_lock`

```cpp
mtx.lock();

std::lock_guard<std::mutex> lock(mtx, std::adopt_lock);
```

`adopt_lock` 表示：

> 互斥锁已经由当前线程锁定，锁管理器只负责以后自动解锁。

如果互斥锁原来没有被锁定，却使用 `adopt_lock`，会产生未定义行为。

------

# 十一、死锁

死锁是多个线程永久等待彼此释放资源。

例如：

```cpp
std::mutex mtx1;
std::mutex mtx2;

void func1()
{
    std::lock_guard<std::mutex> lock1(mtx1);
    std::lock_guard<std::mutex> lock2(mtx2);
}

void func2()
{
    std::lock_guard<std::mutex> lock2(mtx2);
    std::lock_guard<std::mutex> lock1(mtx1);
}
```

可能出现：

```text
线程1持有 mtx1，等待 mtx2
线程2持有 mtx2，等待 mtx1
```

两个线程永久等待。

------

## 使用 `std::lock` 避免多个锁死锁

```cpp
void func()
{
    std::unique_lock<std::mutex> lock1(mtx1, std::defer_lock);
    std::unique_lock<std::mutex> lock2(mtx2, std::defer_lock);

    std::lock(lock1, lock2);

    // 同时持有两个锁
}
```

C++11 没有 `std::scoped_lock`，它是 C++17 加入的。

C++11 中多个锁推荐使用：

```cpp
std::lock()
```

------

## 避免死锁的常见原则

1. 固定加锁顺序。
2. 不要在持锁期间执行耗时操作。
3. 不要在持锁期间调用不可控的外部函数。
4. 尽量减少锁的数量。
5. 使用 `std::lock` 同时获得多个锁。
6. 临界区尽可能短。

------

# 十二、`std::condition_variable`：条件变量

条件变量用于线程之间的等待和通知。

典型场景：

```text
消费者发现队列为空
    ↓
进入等待状态

生产者添加任务
    ↓
通知消费者
```

条件变量必须与以下类型配合：

```cpp
std::unique_lock<std::mutex>
```

------

## 1. 基本成员

```cpp
std::condition_variable cv;
```

等待：

```cpp
cv.wait(lock);
```

通知一个线程：

```cpp
cv.notify_one();
```

通知所有线程：

```cpp
cv.notify_all();
```

------

## 2. 推荐使用带谓词的 `wait`

```cpp
cv.wait(lock, []()
{
    return 条件成立;
});
```

例如：

```cpp
std::mutex mtx;
std::condition_variable cv;
bool ready = false;

void worker()
{
    std::unique_lock<std::mutex> lock(mtx);

    cv.wait(lock, []()
    {
        return ready;
    });

    std::cout << "开始工作\n";
}
```

主线程：

```cpp
{
    std::lock_guard<std::mutex> lock(mtx);
    ready = true;
}

cv.notify_one();
```

------

## 3. `wait()` 内部做了什么

```cpp
cv.wait(lock, predicate);
```

大致相当于：

```cpp
while (!predicate())
{
    cv.wait(lock);
}
```

当条件不满足时：

```text
1. 自动释放 mutex
2. 当前线程进入等待
3. 收到通知后重新竞争 mutex
4. 获得 mutex 后重新检查条件
5. 条件成立后继续执行
```

必须重新检查条件，是为了处理：

- 虚假唤醒
- 多个线程竞争
- 条件被其他线程再次修改

------

# 十三、生产者—消费者模型

```cpp
class TaskQueue
{
public:
    explicit TaskQueue(std::size_t maxSize)
        : _maxSize(maxSize)
    {
    }

    void put(int task)
    {
        std::unique_lock<std::mutex> lock(_mutex);

        _notFull.wait(lock, [this]()
        {
            return _queue.size() < _maxSize;
        });

        _queue.push(task);

        lock.unlock();
        _notEmpty.notify_one();
    }

    int take()
    {
        std::unique_lock<std::mutex> lock(_mutex);

        _notEmpty.wait(lock, [this]()
        {
            return !_queue.empty();
        });

        int task = _queue.front();
        _queue.pop();

        lock.unlock();
        _notFull.notify_one();

        return task;
    }

private:
    std::size_t _maxSize;
    std::queue<int> _queue;

    std::mutex _mutex;
    std::condition_variable _notFull;
    std::condition_variable _notEmpty;
};
```

通知关系：

```text
生产者添加任务
    ↓
队列变为非空
    ↓
通知消费者 _notEmpty

消费者取走任务
    ↓
队列变为非满
    ↓
通知生产者 _notFull
```

------

# 十四、`std::once_flag` 和 `std::call_once`

用于保证某段代码在多个线程中只执行一次。

```cpp
std::once_flag flag;

void init()
{
    std::call_once(flag, []()
    {
        std::cout << "只初始化一次\n";
    });
}
```

即使多个线程同时调用 `init()`，Lambda 也只会成功执行一次。

常用于：

- 单例初始化
- 配置文件加载
- 网络库初始化
- 全局资源初始化

------

## 单例示例

```cpp
class Singleton
{
public:
    static Singleton* getInstance()
    {
        std::call_once(_flag, []()
        {
            _instance = new Singleton;
        });

        return _instance;
    }

private:
    Singleton() = default;

    static Singleton* _instance;
    static std::once_flag _flag;
};

Singleton* Singleton::_instance = nullptr;
std::once_flag Singleton::_flag;
```

不过 C++11 更推荐局部静态单例：

```cpp
class Singleton
{
public:
    static Singleton& getInstance()
    {
        static Singleton instance;
        return instance;
    }

private:
    Singleton() = default;
};
```

C++11 保证局部静态变量初始化是线程安全的。

------

# 十五、`std::future`：获取异步结果

普通线程函数不能直接通过 `join()` 获取返回值。

例如：

```cpp
int add(int a, int b)
{
    return a + b;
}
```

可以使用 `std::async` 和 `std::future`：

```cpp
#include <future>

int main()
{
    std::future<int> result =
        std::async(std::launch::async, add, 10, 20);

    std::cout << result.get() << '\n';
}
```

输出：

```text
30
```

------

## `future::get()`

```cpp
int value = result.get();
```

特点：

- 如果任务没有完成，当前线程会等待
- 获取任务返回值
- 如果异步任务抛出异常，`get()` 会重新抛出该异常
- 一个普通 `future` 通常只能调用一次 `get()`

------

## `future::wait()`

只等待任务完成，不获取结果：

```cpp
result.wait();
```

------

## `wait_for()`

等待一段时间：

```cpp
auto status = result.wait_for(std::chrono::seconds(1));

if (status == std::future_status::ready)
{
    std::cout << result.get() << '\n';
}
```

可能的状态：

```cpp
std::future_status::ready
std::future_status::timeout
std::future_status::deferred
```

------

# 十六、`std::async`

`std::async` 用于启动异步任务。

```cpp
auto result = std::async(std::launch::async, func);
```

启动策略主要有两种。

## 1. `std::launch::async`

明确要求异步执行：

```cpp
auto result =
    std::async(std::launch::async, func);
```

通常会创建新线程执行任务。

------

## 2. `std::launch::deferred`

延迟执行：

```cpp
auto result =
    std::async(std::launch::deferred, func);
```

任务不会立即执行，而是在调用：

```cpp
result.get();
```

或：

```cpp
result.wait();
```

时，由调用线程执行。

------

## 3. 不指定策略

```cpp
auto result = std::async(func);
```

实现可以自行选择：

- 异步执行
- 延迟执行

需要明确异步执行时，建议写：

```cpp
std::launch::async
```

------

# 十七、`std::promise`

`std::promise` 用于在线程之间主动传递结果。

```cpp
#include <future>
#include <thread>

void work(std::promise<int>& promise)
{
    int result = 100;
    promise.set_value(result);
}

int main()
{
    std::promise<int> promise;
    std::future<int> future = promise.get_future();

    std::thread t(work, std::ref(promise));

    std::cout << future.get() << '\n';

    t.join();
}
```

数据传递关系：

```text
promise 负责设置结果
        ↓
共享状态
        ↓
future 负责获取结果
```

------

## 传递异常

```cpp
try
{
    throw std::runtime_error("任务失败");
}
catch (...)
{
    promise.set_exception(std::current_exception());
}
```

接收线程调用：

```cpp
future.get();
```

时会重新抛出异常。

------

# 十八、`std::packaged_task`

`std::packaged_task` 用于把一个可调用对象包装成异步任务，并通过 `future` 获取结果。

```cpp
#include <future>
#include <thread>

int add(int a, int b)
{
    return a + b;
}

int main()
{
    std::packaged_task<int(int, int)> task(add);

    std::future<int> result = task.get_future();

    std::thread t(std::move(task), 10, 20);

    std::cout << result.get() << '\n';

    t.join();
}
```

常用于：

- 线程池
- 任务队列
- 延迟执行
- 将普通函数包装成可返回结果的任务

------

# 十九、`future`、`promise` 和 `packaged_task` 的关系

| 工具            | 作用                                 |
| --------------- | ------------------------------------ |
| `future`        | 获取异步结果                         |
| `promise`       | 手动设置异步结果                     |
| `packaged_task` | 包装函数，自动把函数结果写入共享状态 |
| `async`         | 简化异步任务创建，直接返回 `future`  |

可以简单理解为：

```text
async
    = 帮你创建任务并返回 future

promise
    = 手动向 future 发送结果

packaged_task
    = 包装函数，执行后自动生成结果
```

------

# 二十、`std::atomic`：原子操作

原子类型用于实现不可分割的操作。

```cpp
#include <atomic>

std::atomic<int> count{0};

void add()
{
    for (int i = 0; i < 100000; ++i)
    {
        ++count;
    }
}
```

多个线程执行 `++count` 时，不会产生普通整数的数据竞争。

------

## 常见原子操作

```cpp
std::atomic<int> value{0};

value.store(10);

int x = value.load();

value.fetch_add(1);

value.fetch_sub(1);

value.exchange(100);
```

比较并交换：

```cpp
int expected = 10;

value.compare_exchange_strong(expected, 20);
```

------

## 原子变量与互斥锁区别

| 特性           | `atomic`       | `mutex`      |
| -------------- | -------------- | ------------ |
| 适合数据       | 简单变量       | 复杂数据结构 |
| 是否阻塞       | 通常不阻塞     | 可能阻塞     |
| 使用难度       | 简单操作较方便 | 通用性强     |
| 可保护多步逻辑 | 通常不适合     | 适合         |
| 可保护容器     | 不可以直接保护 | 可以         |

例如：

```cpp
std::atomic<int>
```

适合保护简单计数器。

但下面这种复合操作仍然通常需要互斥锁：

```cpp
if (!queue.empty())
{
    queue.pop();
}
```

因为它包含多个相关步骤。

------

# 二十一、线程安全的核心原则

## 1. 共享数据必须同步

以下情况通常需要同步：

```text
多个线程访问同一个对象
并且至少一个线程会修改它
```

------

## 2. 判断和修改必须放在同一个临界区

错误：

```cpp
if (!isEmpty())
{
    queue.pop();
}
```

即使 `isEmpty()` 内部加锁，也不能保证 `pop()` 时队列仍然非空。

正确：

```cpp
std::lock_guard<std::mutex> lock(mtx);

if (!queue.empty())
{
    queue.pop();
}
```

------

## 3. 临界区尽可能短

不推荐：

```cpp
std::lock_guard<std::mutex> lock(mtx);

执行耗时计算;
网络请求;
文件读写;
休眠;
```

锁持有时间越长，线程竞争越严重。

------

## 4. 不要忘记线程生命周期

每个线程最终必须：

```cpp
join()
```

或者：

```cpp
detach()
```

通常优先使用 `join()`。

------

## 5. 不要在多个线程中无保护地输出

多个线程同时执行：

```cpp
std::cout << ...;
```

输出内容可能互相交叉。

可以用互斥锁保护：

```cpp
std::mutex coutMutex;

{
    std::lock_guard<std::mutex> lock(coutMutex);
    std::cout << "线程输出\n";
}
```

------

# 二十二、常见错误总结

## 错误一：线程未 `join` 或 `detach`

```cpp
std::thread t(func);
```

线程对象析构时会调用 `std::terminate()`。

------

## 错误二：无保护访问共享变量

```cpp
int count = 0;

++count;
```

多个线程同时执行会产生数据竞争。

------

## 错误三：把 `volatile` 当成线程同步工具

```cpp
volatile int count;
```

`volatile` 不能保证：

- 原子性
- 线程同步
- 内存可见性
- 操作顺序

线程同步应使用：

```cpp
mutex
atomic
condition_variable
```

------

## 错误四：条件变量没有使用谓词

不推荐：

```cpp
cv.wait(lock);
```

推荐：

```cpp
cv.wait(lock, []()
{
    return condition;
});
```

------

## 错误五：条件状态未被同一把锁保护

错误：

```cpp
ready = true;
cv.notify_one();
```

而消费者使用另一把锁检查 `ready`。

共享条件和条件变量应配合同一个互斥锁。

------

## 错误六：分别给检查和操作加锁

错误：

```cpp
if (!isEmpty())
{
    pop();
}
```

检查与修改必须在同一个锁作用域内。

------

## 错误七：锁顺序不一致

```text
线程1：先锁 A，再锁 B
线程2：先锁 B，再锁 A
```

容易产生死锁。

------

# 二十三、C++11 线程库知识体系

可以按以下顺序学习。

## 第一阶段：线程基础

```text
std::thread
join
detach
joinable
线程参数传递
std::ref
成员函数线程
线程移动
```

## 第二阶段：线程同步

```text
std::mutex
lock_guard
unique_lock
try_lock
std::lock
死锁
临界区
数据竞争
```

## 第三阶段：线程通信

```text
condition_variable
wait
notify_one
notify_all
生产者消费者模型
阻塞队列
```

## 第四阶段：异步任务

```text
future
promise
packaged_task
async
launch::async
launch::deferred
```

## 第五阶段：高级并发

```text
atomic
内存序
线程池
无锁队列
任务调度
并发容器
```

------

# 二十四、核心类和函数速查表

| 类或函数                        | 作用               |
| ------------------------------- | ------------------ |
| `std::thread`                   | 创建和管理线程     |
| `join()`                        | 等待线程结束       |
| `detach()`                      | 线程分离           |
| `joinable()`                    | 判断线程是否可连接 |
| `std::this_thread::get_id()`    | 获取当前线程 ID    |
| `std::this_thread::sleep_for()` | 线程休眠一段时间   |
| `std::this_thread::yield()`     | 主动让出 CPU       |
| `std::mutex`                    | 普通互斥锁         |
| `std::recursive_mutex`          | 可递归互斥锁       |
| `std::timed_mutex`              | 支持超时的互斥锁   |
| `std::lock_guard`               | 简单 RAII 锁管理   |
| `std::unique_lock`              | 灵活 RAII 锁管理   |
| `std::lock`                     | 同时锁定多个互斥锁 |
| `std::condition_variable`       | 条件等待和线程通知 |
| `notify_one()`                  | 唤醒一个等待线程   |
| `notify_all()`                  | 唤醒所有等待线程   |
| `std::once_flag`                | 保存一次性执行状态 |
| `std::call_once`                | 保证代码只执行一次 |
| `std::async`                    | 创建异步任务       |
| `std::future`                   | 获取异步结果       |
| `std::promise`                  | 设置异步结果       |
| `std::packaged_task`            | 包装带返回值的任务 |
| `std::atomic`                   | 原子操作           |

------

# 二十五、最重要的几个结论

```text
1. thread 负责创建和管理线程。

2. mutex 负责保护共享数据。

3. lock_guard 适合简单加锁。

4. unique_lock 适合条件变量和复杂锁控制。

5. condition_variable 负责等待和通知，但不负责保护数据。

6. wait 必须配合条件判断，优先使用谓词版本。

7. future 用于获取异步结果。

8. promise 用于主动设置异步结果。

9. packaged_task 常用于线程池任务封装。

10. atomic 适合简单原子变量，不适合替代所有互斥锁。

11. 判断共享状态和修改共享状态必须位于同一个临界区。

12. 每个 thread 对象析构前必须 join 或 detach。
```

可以将 C++11 多线程整体理解为：

```text
std::thread
    负责创建线程

std::mutex
    负责互斥访问

std::condition_variable
    负责线程等待和唤醒

std::future / promise
    负责线程间结果传递

std::atomic
    负责简单无锁原子操作
```